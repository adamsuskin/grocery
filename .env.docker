# Docker Production Environment Variables Template
# Copy this file to .env and fill in with your production values
# NEVER commit the .env file to version control!

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
DB_USER=grocery
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
DB_NAME=grocery_db
DB_PORT=5432
DB_MAX_CONNECTIONS=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# ============================================================================
# JWT CONFIGURATION
# ============================================================================
# Generate secrets with: openssl rand -base64 32
JWT_ACCESS_SECRET=CHANGE_THIS_GENERATE_WITH_OPENSSL
JWT_REFRESH_SECRET=CHANGE_THIS_DIFFERENT_FROM_ACCESS
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
NODE_ENV=production
PORT=3001
CORS_ORIGIN=https://your-domain.com

# ============================================================================
# SECURITY CONFIGURATION
# ============================================================================
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================================================
# ZERO-CACHE CONFIGURATION
# ============================================================================
ZERO_AUTH_SECRET=CHANGE_THIS_GENERATE_WITH_OPENSSL
ZERO_LOG_LEVEL=info

# ============================================================================
# FRONTEND CONFIGURATION
# ============================================================================
VITE_API_URL=https://api.your-domain.com
VITE_ZERO_SERVER=https://sync.your-domain.com
VITE_AUTH_ENABLED=true

# ============================================================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================================================
#
# 1. Generate all secrets using: openssl rand -base64 32
# 2. Use different secrets for JWT_ACCESS_SECRET, JWT_REFRESH_SECRET, and ZERO_AUTH_SECRET
# 3. Set strong database password
# 4. Update CORS_ORIGIN to match your frontend domain
# 5. Update VITE_API_URL and VITE_ZERO_SERVER to production URLs
# 6. Consider using a secret management service (AWS Secrets Manager, HashiCorp Vault)
# 7. Enable HTTPS with reverse proxy (nginx, Traefik, Caddy)
# 8. Set up database backups
# 9. Configure log aggregation and monitoring
# 10. Review and adjust resource limits in docker-compose.prod.yml
#
