/**
 * RequireAuth Component
 *
 * Alternative export name for ProtectedRoute component.
 * Both names can be used interchangeably for route protection.
 *
 * @example
 * import { RequireAuth } from './components/RequireAuth';
 *
 * <RequireAuth>
 *   <Dashboard />
 * </RequireAuth>
 */

export { ProtectedRoute as RequireAuth } from './ProtectedRoute';
export type { ProtectedRouteProps as RequireAuthProps } from './ProtectedRoute';
